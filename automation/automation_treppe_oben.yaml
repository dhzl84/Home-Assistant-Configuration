  - alias: treppe open licht an
    initial_state: 'on'
    trigger:
      platform: state
      entity_id: binary_sensor.motion_sensor_158d0001fa3d9c
      to: 'on'

    condition:
      condition: and
      conditions:
        - condition: template
          value_template: '{{ states.sensor.illumination_158d0001fa3d9c.state | int <= states.input_number.illuminance_threshold_treppe_oben.state | int }}'
        - condition: state
          entity_id: switch.shelly1_02
          state: "off"

    action:
      - service: switch.turn_on
        entity_id: switch.shelly1_02
      - service: input_boolean.turn_on
        entity_id: input_boolean.treppe_oben_licht_wegen_bewegung

##############################################################################################################################################################

  - alias: treppe open licht aus
    initial_state: 'on'
    trigger:
      platform: state
      entity_id: binary_sensor.motion_sensor_158d000200ceec
      to: 'off'
    condition:
      condition: and
      conditions:
        - condition: state
          entity_id: switch.shelly1_02
          state: "on"
        - condition: state
          entity_id: input_boolean.treppe_oben_licht_wegen_bewegung
          state: "on"

    action:
      - service: switch.turn_off
        entity_id: switch.shelly1_02
      - service: input_boolean.turn_off
        entity_id: input_boolean.treppe_oben_licht_wegen_bewegung