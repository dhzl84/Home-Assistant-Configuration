  - alias: 'Zeit einblenden und Sonne ausblenden'
    initial_state: 'on'
    trigger:
      platform: state
      entity_id: input_select.zeitsteuerung
      to: 'Zeit'
    action:
    - service: group.set_visibility
      entity_id: group.beleuchtung_sonne_auto_group
      data:
        visible: false
    - service: group.set_visibility
      entity_id: group.beleuchtung_timer_auto_group
      data:
        visible: true
        
  - alias: 'Sonne einblenden und Zeit ausblenden'
    initial_state: 'on'
    trigger:
      platform: state
      entity_id: input_select.zeitsteuerung
      to: 'Zeit und Sonne'
    action:
    - service: group.set_visibility
      entity_id: group.beleuchtung_timer_auto_group
      data:
        visible: false
    - service: group.set_visibility
      entity_id: group.beleuchtung_sonne_auto_group
      data:
        visible: true
        
  - alias: 'Sonne und Zeit ausblenden'
    initial_state: 'on'
    trigger:
      platform: state
      entity_id: input_select.zeitsteuerung
      to: 'aus'
    action:
    - service: group.set_visibility
      entity_id: group.beleuchtung_timer_auto_group
      data:
        visible: false
    - service: group.set_visibility
      entity_id: group.beleuchtung_sonne_auto_group
      data:
        visible: false