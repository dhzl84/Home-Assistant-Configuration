  - alias: "Bürolicht Animationsgeschwindigkeit"
    initial_state: True
    hide_entity: False
    trigger:
      - platform: state
        entity_id: input_number.buerolicht_animation_speed
    action:
      - service: mqtt.publish
        data_template:
          topic: "licht/buero/set"
          payload: '{"transition":{{ trigger.to_state.state | int }}}'